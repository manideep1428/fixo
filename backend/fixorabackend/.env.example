# --- Django ---
SECRET_KEY=django-insecure-fixora-dev-key-change-me
DEBUG=True

# --- CockroachDB (fill these in with your CockroachCloud credentials) ---
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=26257

# --- Redis (leave empty to use in-memory channel layer / cache for dev) ---
REDIS_URL=
